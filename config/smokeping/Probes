*** Probes ***

+ FPing

binary = /usr/sbin/fping

+ Curl

binary = /usr/bin/curl

+ TCPPing

binary = /usr/bin/tcpping # mandatory

+ DNS

binary = /usr/bin/dig # mandatory
lookup = google.com
pings = 5
step = 180